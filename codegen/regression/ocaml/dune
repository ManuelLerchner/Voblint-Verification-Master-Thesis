(rule
 (targets Voblint_CLI.ml)
 (deps ../../generated/ml/Voblint_CLI.ml)
 (action
  (copy %{deps} %{targets})))

; The export's warnings are the code generator's, so only it is exempt.

(library
 (name codegen_regression_export)
 (wrapped false)
 (modules Voblint_CLI)
 (libraries str zarith)
 (flags
  (:standard -w -a)))

(executable
 (name codegen_regression)
 (modules codegen_regression)
 (libraries codegen_regression_export str zarith))
