# Identifiers this tree has deliberately removed.
#
# `scripts/check_retired_identifiers.py` fails the commit when any of these
# still occurs in a tracked `.thy` file -- in code or in prose. It exists
# because Isabelle does not reject every surviving use of a removed name:
# inside `assumes`, an unknown lowercase identifier silently becomes a free
# variable, so the locale keeps building while its assumption stops saying
# what it was written to say. Deleting `DG_Transfer_Combinators.thy` turned
# three `call_fwd_ok` assumptions into statements about an arbitrary function
# named `enter_local`, and every session stayed green.
#
# Append a name here when you remove an API. Remove it again -- explicitly --
# if a later design deliberately reuses the name.
#
# Matching is whole-word, so `dgs_caller_cont` does not match `caller_cont`.
# Deliberately NOT listed: `caller_cont`, `combine_env`, `combine_assign`.
# Prose across this tree names record fields by their role rather than by the
# `dgs_`-prefixed field name, and `src/Framework/README.md` documents that
# convention; those three are live vocabulary, not dead references.

# --- Pure-pair transfer combinators (DG_Transfer_Combinators.thy) ---
# Replaced by manager-native transfers: a spec's fields are `man_transfer`s,
# and what these projected out of a `'dg \<times> 'dl` pair is now `entered` plus
# the observations in DG_Spec.
enter_local
enter_global
combine_local
combine_global

# --- Old DG_Soundness vocabulary ---
# The surviving spine is `dg_hook_gamma` / `hook_gen` / `hook_postfix`.
dg_cmb
dg_cmb_at
dg_extra
dg_gamma
dg_gen
dg_acc
dg_postfix
dg_enter
dg_trees
dg_spec_commute
sound_dg_hooks_ltr
hook_post_solution_collect_sound_ltr

# --- Pure-pair spec application, superseded by compiled transfer trees ---
apply_dg_spec
apply_dg_spec_at
apply_dg_spec_contribution
apply_dg_spec_contribution_at
dg_edge_tree_hook
dg_enter_tree_hook
dg_combine_tree_hook
dg_spec_combine_tree_at
edge_transfer_of_step
combine_transfer_of_comb
dgs_enter_pair
answer_local
publish_global
lift_dg_step
lift_gamma
indep_dg_spec
local_state_dg_spec_st_for

# --- Unit ownership split, before `ownership_split_transfer_gen` ---
merge_split_spec
merge_split_spec_exec
unit_step_st
unit_combine_step_env_for
unit_combine_step_assign_for
unit_combine_step_st_env
unit_combine_step_st_assign_for

# --- Placement policy (removed; see docs/GOBLINT_ALIGNMENT_REGISTER.md) ---
# Current Goblint exposes no analogous framework abstraction, so this was
# closed by removal rather than migrated.
placed_dg_exec_analysis
project_component
project_placed_resolved
ownership_split_dg_spec_placed
ownership_split_dg_spec_placed_st
unit_step_placed
unit_step_placed_st
unit_combine_step_env_placed
unit_combine_step_assign_placed
unit_combine_step_placed_st_env
unit_combine_step_placed_st_assign

# --- Routed contribution hooks, folded into `routed_cmb_g` ---
routed_cmb_g_contribution
routed_cmb_g_contribution_at

# --- Dead-code lift (DG_Dead_Code_Lift.thy) ---
dead_code_lift
dead_code_normalize

# --- Concretization helpers removed with the pair interface ---
gammaM_exec
gamma_join
gamma_join_exec
cs_route_commute
ics_route_commute
ectx_spec
ectx_abs_spec
ectx_gamma
ectx_gamma_Bot
ectx_gamma_eq
ectx_sound_exec
entered_ectx_spec_eq_entry_state_entered
ictx_spec
ictx_abs_spec
ictx_gamma
ictx_sound_exec
base_dg_spec_for
base_dg_spec_for_lifted
base_dg_spec_st_for_lifted
base_dg_spec_sound
base_dg_spec_for_sound
gamma_dg_base
base_dg_exec_analysis
unit_transfer
unit_transfer_gen
unit_combine_transfer
unit_dg_spec_for
unit_dg_spec_st_for
gamma_unit
sound_dg_spec_unit_for
unit_dg_exec_analysis
ownership_split_step_for
ictx_eqs
ictx_sol
ictx_sol_prog
ictx_terminates
ictx_terminates_prog
monovariant_analysis_result_for
seqcomp_tree
seqcomp_mono
seqcomp_assoc
seqcomp_Answer_right
env_indep_deps_seqcomp
traverse_seqcomp
traverse_rhs_seqcomp
dep_aux_seqcomp
sides_of_rhs_seqcomp
sp_local
sp_global
sp_at
sp_sideg
read_local
read_global
read_at
side_effect

# --- sp_run renamed to sp_compile: it compiles a program to a tree, it does
# not execute one ---
sp_run
sp_run_def
sp_run_with
sp_run_with_def
sp_run_with_bind
sp_run_with_bind_read_local
sp_run_with_bind_read_global
sp_run_with_bind_publish
sp_run_with_bind_read_at
sp_run_with_read_local
sp_run_with_read_global
sp_run_with_publish
sp_run_with_read_at
sp_run_with_return
sp_run_sp_lift_tree

# --- se_constraint_holds renamed to tree_covered_at: names the covering
# obligation the tree states, not an unexplained abbreviation ---
se_constraint_holds
se_constraint_holds_def
se_constraint_holds_local
se_constraint_holds_sides
part_post_solution_imp_se_constraint_holds
part_post_solution_iff_se_constraint_holds

# --- fold_rhs_trees renamed to fold_rhs_contributions: the function returns
# a strategy_program, not a tree ---
fold_rhs_trees
dep_aux_fold_rhs_trees_char
sides_of_rhs_fold_rhs_trees_char
sides_of_rhs_fold_rhs_trees_bot
traverse_fold_rhs_trees_char
fold_rhs_trees_side_rhs_fold_dg_global_char
fold_rhs_trees_side_rhs_fold_dg_local_char

# --- solver_menu/run_menu removed: demo-only convenience for running all four
# update rules at once, used only under Examples. Solver dispatch for the CLI
# already exists independently as analyse_with_solver (Analyse_Dispatch); the
# example call sites now name each concrete TD_side_<rule>_Interp_solve
# directly. TD_Solver_Menu.thy stays as a facade import for TD_Solver_Bridge. ---
side_solver
solver_menu
run_menu

# --- gamma_state_witness_not_bot renamed: the theory explicitly distinguishes
# semantic emptiness (gamma_state = {}) from structural bottom, and the old
# name suggested the latter. The lemma proves the former. ---
gamma_state_witness_not_bot

# --- gamma_bfilter_lifted / gamma_branch_lifted removed: bfilter_lifted is no
# longer bfilter's thin normalize_lift wrapper, so its concretization is no
# longer equal to plain bfilter's -- only bounded by it (bfilter_lifted_sound,
# branch_lifted_sound). An equality lemma of the old shape would be false. ---
gamma_bfilter_lifted
gamma_branch_lifted

# --- canonicalize_state removed: it existed only to fix bfilter's And/Or
# join-arm pollution, which would have made plain bfilter's own equations
# depend on the non-executable is_empty_state. bfilter_lifted fixes the
# pollution instead, at the lifted level, by returning structural Bot
# directly -- canonicalize_state had no remaining consumer. ---
canonicalize_state
canonicalize_state_bot
canonicalize_state_id
canonicalize_state_le
canonicalize_state_gamma
canonicalize_state_mono
canonicalize_state_idem

# --- footprint_exp / probe_exp / afilter_st_locality / bfilter_st_bot /
# bfilter_st_locality removed (Exec_Backward.thy): this whole chain existed
# solely to support bfilter_st_lift's old whole-expression witness-bottom
# probe after a plain, possibly-polluted join. bfilter_st_lift's And-False/
# Or-True equations now gate each arm to structural Bot before joining,
# exactly mirroring bfilter_lifted, so no probe is needed. ---
footprint_exp
probe_exp
afilter_st_locality
bfilter_st_bot
bfilter_st_locality
lift_probe_step
bfilter_lift_step
