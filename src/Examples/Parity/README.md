# Examples / Parity

Parity-domain witness: the second-domain validation of the domain-registration API.

| File | Role | What |
| --- | --- | --- |
| `Example_Parity_DG_Flagship.thy` | canonical spine | parity analysis of an even-step loop, executed and certified on the D/G spine; registers through `unit_dg_exec_analysis` (`parity_reg` in `DG_Domain_Registration`) with no copied `Hstep`/`Hcomb` proofs |

Role vocabulary: repository `README.md`.
