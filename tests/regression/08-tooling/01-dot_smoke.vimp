// PARAM: --analysis interval --dot
// DOT-rendering smoke test: same shape as 00-sanity/01-straight_line_proved.vimp,
// run with --dot instead of the textual report.
program {
  void main() {
    x := 5;
    __voblint_check(0 < x)
  }
}
