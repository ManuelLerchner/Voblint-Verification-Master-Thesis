// PARAM: --analysis interval --solver warrow
// Same program as 01-interval_join.vimp, solved with Interval's production
// default solver (Solver_Warrow) instead of join -- confirms --solver warrow
// reproduces the plain default CLI invocation's verdict.
void main() {
  int32 x;
  x := 5;
  __voblint_check(0 < x) // PROVED
}
