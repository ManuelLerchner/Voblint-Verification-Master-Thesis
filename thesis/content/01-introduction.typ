= Introduction <ch:intro>

TODO. The pipeline figure belongs here, early: it states what is proved and
what is trusted before any detail arrives. (Its prototype is `fig:pipeline` in
the draft-only figure gallery; build with `--input gallery=1` to see it.)
